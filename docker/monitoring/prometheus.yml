# Global scrape settings
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "devops-task-manager-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:4000"]
  - job_name: "node-exporter"
    static_configs:
      - targets: ["node-exporter:9100"]
