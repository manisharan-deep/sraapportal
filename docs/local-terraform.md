# Local Terraform Example

Use Terraform locally to generate artifacts or configuration files.

```
cd terraform/local
terraform init
terraform apply
```

This creates `local-info.txt` in the same directory.
